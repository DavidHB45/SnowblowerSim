# Blower models — Sketchfab recon

**Not yet generated.** `api.sketchfab.com` is blocked by the cloud container's network policy
(HTTP 403 from the egress proxy), so the search could not run there.

Run it locally (Python 3, standard library only). This file will be overwritten with the results table:

```
python3 scripts/sketchfab-search.py
```

Table columns: Name | URL | License (CC0 / CC-BY only) | Triangles | PBR textures | Uploader | Fit score (1–5, filled in by hand).
Queries: "snowblower", "snow blower", "snow thrower", "two stage snowblower", "tracked snowblower", "three stage snowblower".
